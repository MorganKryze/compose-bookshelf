# Sure Environment Variables
# Please copy this file to .env and set the appropriate values
# execute: 'cp .env.example .env'

# Default docker settings
# Id and group for file permissions
PUID=1000
PGID=1000
# Timezone setting
TZ=Europe/London
# Docker volumes path
DOCKER_VOLUMES_PATH=/mnt/pool/docker-volumes

# This is the domain where your Sure instance will be accessible
# e.g., sure.your-domain.com
APP_DOMAIN=sure.your-domain.com

# Database Configuration
POSTGRES_USER=sure_user
POSTGRES_DB=sure_production
POSTGRES_PASSWORD=very-long-32-character-random-password

# Application secret key base
SECRET_KEY_BASE=a-very-long-random-secret-key-base-for-rails-application

# Optional OpenAI Access Token for AI features
# OPENAI_ACCESS_TOKEN=

# SMTP Configuration
# This is only needed if you intend on sending emails from your Maybe instance (such as for password resets or email financial reports).
SMTP_ADDRESS=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_TLS_ENABLED=
EMAIL_SENDER=

