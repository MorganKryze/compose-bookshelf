# Code-server Environment Variables
# Please copy this file to .env and set the appropriate values
# execute: 'cp .env.example .env'

# Default docker settings
# Id and group for file permissions
PUID=1000
PGID=1000
# Timezone setting
TZ=Europe/Paris
# Docker volumes path
DOCKER_VOLUMES_PATH=/mnt/pool/docker/volumes

# Path to docker related files, not the docker install but compose files and volumes
DOCKER_PATH=/mnt/pool/docker
# The same way, you may add several directories to be manaeged and accessed by code-server
# Example:
# EXTRA_PATH_1="/mnt/pool/images"
# EXTRA_PATH_2="/mnt/pool/videos"