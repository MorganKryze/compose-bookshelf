services:
  wizarr:
    image: ghcr.io/wizarrrr/wizarr
    container_name: wizarr
    restart: unless-stopped
    environment:
      PUID: ${PUID}
      PGID: ${PGID}
      TZ: ${TZ}
      DISABLE_BUILTIN_AUTH: ${DISABLE_BUILTIN_AUTH}
    security_opt:
      - no-new-privileges:true
    volumes:
      - ${DOCKER_VOLUMES_PATH}/wizarr:/data
    expose:
      - 5690
    networks:
      - pangolin
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
        reservations:
          cpus: '0.10'
          memory: 128M

networks:
  pangolin:
    external: true